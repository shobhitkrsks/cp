#include <bits/stdc++.h>
using namespace std;

vector<vector<int>> palindromePairs(vector<string> &words)
{
}

int main()
{
    int n;
    cin >> n;

    vector<string> words;
    string word;
    for (int i = 0; i < n; i++)
    {
        cin >> word;
        words.push_back(word);
    }

    return 0;
}